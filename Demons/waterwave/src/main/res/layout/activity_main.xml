<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
                android:background="#000000"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <Button
        android:id="@+id/wave_demo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="标准正余弦波纹"
        android:textSize="20dp" />

    <Button
        android:id="@+id/xfermode_demo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/wave_demo"
        android:gravity="center"
        android:text="图像混合模式之遮罩实现圆形波纹"
        android:textSize="20dp" />

    <Button
            android:id="@+id/progressbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/xfermode_demo"
            android:gravity="center"
            android:text="菊花"
            android:textSize="20dp" />

    <com.example.heyulong.test.CusProgressbar
            android:id="@+id/iv"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/progressbar"
            android:layout_width="30dp"
            android:layout_height="30dp"/>

</RelativeLayout>